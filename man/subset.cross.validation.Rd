% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross-validation.R
\name{subset.cross.validation}
\alias{subset.cross.validation}
\title{Subset (random) cross validation for classification methods}
\usage{
\method{subset}{cross.validation}(data, y, vars, trials, split, model, K = NULL, kernal = NULL)
}
\arguments{
\item{data}{A data.frame containing predictors and a response.}

\item{y}{Character scalar; name of the response column in \code{data} (factor for classification).}

\item{vars}{Character vector; names of predictor columns to use.}

\item{trials}{Integer; number of random splits to perform.}

\item{split}{Numeric in (0,1); proportion of observations used for training.}

\item{model}{Character; one of "LDA", "QDA", "GLM", or "KNN".}

\item{K}{Integer; required only for KNN; nearest neighbors to use.}

\item{kernal}{Character; kernel to use for KNN (e.g., "rectangular", "triangular").}
}
\value{
A numeric vector of length \code{trials} containing accuracy on the test set for each trial.
}
\description{
A wrapper to repeatedly (randomly) split data into training/test sets and compute
classification accuracy for LDA, QDA, GLM (logistic) and KNN (via kknn).
}
\examples{
\dontrun{
subset.cross.validation(
  iris, y = "Species",
  vars = c("Sepal.Length", "Sepal.Width"),
  trials = 10, split = 0.7, model = "GLM"
)}
}
