% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling.R
\name{mcmc_sampling}
\alias{mcmc_sampling}
\title{Simple Metropolis MCMC sampler (1D)}
\usage{
mcmc_sampling(pdf, proposal, start, n)
}
\arguments{
\item{pdf}{A function that returns non-negative density values (not necessarily normalized).}

\item{proposal}{A function of one argument (current state) that returns a proposed next state.}

\item{start}{Numeric scalar giving the starting location of the chain.}

\item{n}{Integer; number of iterations (length of returned vector).}
}
\value{
Numeric vector of length \code{n} containing the Markov chain.
}
\description{
A straightforward Metropolis algorithm for a univariate target density \code{pdf}.
The proposal mechanism should be a function that accepts the current state
and returns a proposed state (i.e. it may draw randomness internally).
}
\examples{
target_pdf <- function(x) dnorm(x)
prop <- function(x) rnorm(1, mean = x, sd = 0.5)
chain <- mcmc_sampling(target_pdf, prop, start = 0, n = 1000)
}
